import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tempstox.studentlibrary',
  appName: 'Student Library',
  webDir: 'dist'
};

export default config;
