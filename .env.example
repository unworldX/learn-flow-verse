# ============================================
# ENVIRONMENT VARIABLES TEMPLATE
# ============================================
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env or .env.local to git!
# ============================================

# Supabase Configuration (REQUIRED)
# Get these from: https://app.supabase.com/project/YOUR_PROJECT/settings/api
VITE_SUPABASE_PROJECT_ID="your-project-id-here"
VITE_SUPABASE_URL="https://your-project-id.supabase.co"

# Use EITHER publishable key OR anon key (both work the same for frontend)
VITE_SUPABASE_PUBLISHABLE_KEY="your-anon-key-here"
VITE_SUPABASE_ANON_KEY="your-anon-key-here"

# ============================================
# ⚠️ SECURITY WARNING - DO NOT USE THESE IN FRONTEND
# ============================================
# The following should NEVER be in .env with VITE_ prefix:
# - VITE_SUPABASE_SERVICE_ROLE_KEY (NEVER expose this to frontend!)
# - VITE_SUPABASE_EMAIL (User credentials - login at runtime instead)
# - VITE_SUPABASE_PASSWORD (NEVER hardcode passwords!)
# - TOKEN (Tokens are obtained after authentication, not stored in .env)
# ============================================

# Optional: For development scripts only (NOT used by frontend)
# These are only used by scripts/print-supabase-keys.mjs
# Leave empty and authenticate through the app instead
VITE_SUPABASE_EMAIL=""
VITE_SUPABASE_PASSWORD=""nv and fill with your project values
VITE_SUPABASE_PROJECT_ID="your-project-ref"
VITE_SUPABASE_URL="https://your-project-ref.supabase.co"
# Either key name works; prefer PUBLISHABLE_KEY if you’ve created one
VITE_SUPABASE_PUBLISHABLE_KEY="your-anon-or-publishable-key"
VITE_SUPABASE_ANON_KEY="your-anon-key"

# Optional utilities (used by scripts/print-supabase-keys)
VITE_SUPABASE_EMAIL=""
VITE_SUPABASE_PASSWORD=""
# Do NOT store access tokens in .env – obtained at runtime after login
# TOKEN=""
